name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  go:
    uses: Glyndor/.github/.github/workflows/go-ci.yml@aba530bccc10ed0f1eb93b063824560ec9347ea7 # v1.3.0
    with:
      coverage-threshold: 90
